<% @entis.each do |enti| %>
<div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">

    <p><%= enti.text %></p>


</div></p>
        </div>
        <a class="waves-effect waves-light btn">SHOW</a>
        <% if user_signed_in? && current_user.id == enti.user_id %>
        <div class="more">

          <ul class="more_list">
            <li>
              <%= link_to 'EDIT', "/entis/#{enti.id}/edit", method: :get %>
            </li>
            <li>
              <%= link_to 'DESTOROY', "/entis/#{enti.id}", method: :delete %>
            </li>
          </ul>
        </div>
      <% end %>
      </div>
    </div>
  </div>
<% end %>
